# 环境变量配置示例文件
# 将此文件复制为 .env.local 并填入你的实际 token

# GitHub Personal Access Token (需要 repo 权限)
# 从 https://github.com/settings/tokens 获取
GH_TOKEN=your_github_token_here

# 备选：也可以使用 GITHUB_TOKEN (electron-updater 会自动检测)
# GITHUB_TOKEN=your_github_token_here

# 注意：
# 1. 请确保你的 token 有 'repo' 权限
# 2. 对于私有仓库，这个 token 是必需的
# 3. 请不要将真实的 token 提交到版本控制系统中 